{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Ingestion",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/services/ingestion.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}